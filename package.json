{
  "name": "tsfunc",
  "version": "1.0.0",
  "description": "poc to understand best layout for functions",
  "main": "./dist/index.js",
  "types": "./dist/index.d.js",
  "scripts": {
    "lint": "",
    "test": "yarn jest",
    "watch": "yarn jest --watchAll",
    "build": "tsc --build tslib",
    "clean": "rm -rf dist",
    "rebuild": "yarn clean  && yarn build"
  },
  "devDependencies": {
    "@types/jest": "^24.0.18",
    "@types/node": "^12.7.9",
    "jest": "^24.9.0",
    "standard": "^14.3.1",
    "typescript": "^3.6.3",
    "ts-jest": "^24.1.0"
  },
  "dependencies": {
    "@azure/functions": "^1.0.1-beta1"
  }
}
